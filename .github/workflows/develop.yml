name: develop

on:
  push:
    branches: [develop]

jobs:
  tests:
    uses: ./.github/workflows/ci-tests.yml

  build-push:
    needs: tests
    uses: ./.github/workflows/ci-build-push.yml
    secrets: inherit
